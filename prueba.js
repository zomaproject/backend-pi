
const array = [1, 2, 3, 4, 5];

const newArray = array.filter(x => x > 3 || x < 2)

console.log(newArray); // [1, 4, 5]

const hola = 'hola'

const no = async (source) => {
  simon =  source === 'simon' ? await simon(hola) :await simon();  
}